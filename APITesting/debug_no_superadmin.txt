============================= test session starts =============================
platform win32 -- Python 3.13.9, pytest-9.0.1, pluggy-1.6.0 -- C:\Users\AravindMaddineni\AppData\Local\Microsoft\WindowsApps\PythonSoftwareFoundation.Python.3.13_qbz5n2kfra8p0\python.exe
cachedir: .pytest_cache
metadata: {'Python': '3.13.9', 'Platform': 'Windows-11-10.0.26200-SP0', 'Packages': {'pytest': '9.0.1', 'pluggy': '1.6.0'}, 'Plugins': {'html': '4.1.1', 'metadata': '3.1.1'}}
rootdir: C:\Users\AravindMaddineni\Downloads\Apitest2 1\APITesting\APITesting
configfile: pytest.ini
plugins: html-4.1.1, metadata-3.1.1
collecting ... collected 30 items

tests/test_update_profile_ddt.py::test_update_profile_ddt[valid_update_all_fields] DEBUG: Trying default password 'Aravind@123'...
DEBUG: Login failed for 'Aravind@123'. Status: 404, Body: {"error": "User not found."}
DEBUG: Default login failed. Attempting rescue with known passwords...
DEBUG: Trying candidate 'Aravind@1234'...
DEBUG: Login failed for 'Aravind@1234'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'Aravind@12345'...
DEBUG: Login failed for 'Aravind@12345'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'ThinxView@2025'...
DEBUG: Login failed for 'ThinxView@2025'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'P@sswørd123'...
DEBUG: Login failed for 'P@sswørd123'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'P@sswørd123'...
DEBUG: Login failed for 'P@sswørd123'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'P@sswrd123'...
DEBUG: Login failed for 'P@sswrd123'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'LongPassword123!@#$%'...
DEBUG: Login failed for 'LongPassword123!@#$%'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'Pa$$w0rd!'...
DEBUG: Login failed for 'Pa$$w0rd!'. Status: 404, Body: {"error": "User not found."}
DEBUG: Trying candidate 'Valid123!'...
DEBUG: Login failed for 'Valid123!'. Status: 404, Body: {"error": "User not found."}
ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[no_changes_detected] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_all_fields] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_name] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_email] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_phone] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[name_whitespace_only] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[name_single_char] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[name_max_length_exceeded] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[name_with_numbers] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[name_with_special_chars] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_format] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_no_at] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_no_dot] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_domain] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[email_uppercase] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_alpha] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_special_chars] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_short] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_long] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[phone_starts_with_0] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[phone_all_zeros] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[payload_with_extra_fields] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[payload_null_values] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[payload_incorrect_types] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[empty_string_name] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[empty_string_phone] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[empty_string_email] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[update_phone_only] ERROR
tests/test_update_profile_ddt.py::test_update_profile_ddt[unauthorized_no_token] ERROR

=================================== ERRORS ====================================
_____ ERROR at setup of test_update_profile_ddt[valid_update_all_fields] ______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[no_changes_detected] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
________ ERROR at setup of test_update_profile_ddt[missing_all_fields] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
___________ ERROR at setup of test_update_profile_ddt[missing_name] ___________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
__________ ERROR at setup of test_update_profile_ddt[missing_email] ___________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
__________ ERROR at setup of test_update_profile_ddt[missing_phone] ___________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[name_whitespace_only] _______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_________ ERROR at setup of test_update_profile_ddt[name_single_char] _________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_____ ERROR at setup of test_update_profile_ddt[name_max_length_exceeded] _____

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
________ ERROR at setup of test_update_profile_ddt[name_with_numbers] _________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_____ ERROR at setup of test_update_profile_ddt[name_with_special_chars] ______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[invalid_email_format] _______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[invalid_email_no_at] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[invalid_email_no_dot] _______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[invalid_email_domain] _______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_________ ERROR at setup of test_update_profile_ddt[email_uppercase] __________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[invalid_phone_alpha] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
___ ERROR at setup of test_update_profile_ddt[invalid_phone_special_chars] ____

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[invalid_phone_short] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
________ ERROR at setup of test_update_profile_ddt[invalid_phone_long] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[phone_starts_with_0] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_________ ERROR at setup of test_update_profile_ddt[phone_all_zeros] __________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
____ ERROR at setup of test_update_profile_ddt[payload_with_extra_fields] _____

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_______ ERROR at setup of test_update_profile_ddt[payload_null_values] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
_____ ERROR at setup of test_update_profile_ddt[payload_incorrect_types] ______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
________ ERROR at setup of test_update_profile_ddt[empty_string_name] _________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
________ ERROR at setup of test_update_profile_ddt[empty_string_phone] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
________ ERROR at setup of test_update_profile_ddt[empty_string_email] ________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
________ ERROR at setup of test_update_profile_ddt[update_phone_only] _________

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
______ ERROR at setup of test_update_profile_ddt[unauthorized_no_token] _______

    @pytest.fixture(scope="session")
    def auth_token():
        """
        Obtain auth token with self-healing logic.
        If default login fails, it tries known passwords, resets to default, and retries.
        """
        login_url = "https://authentication.thinxview.com/api/auth/login/"
        reset_url = "https://r2xywlbqc9.execute-api.ap-south-1.amazonaws.com/api/profile/aravind.m@gndsolutions.in/thinxfresh/update-password"
    
        default_pwd = "Aravind@123"
        # List of passwords used in tests to try if default fails
        known_passwords = [
            default_pwd,
            "Aravind@1234",      # New from rescue
            "Aravind@12345",
            "ThinxView@2025",
            "P@ssw\u00F8rd123",  # unicode escaped
            "P@sswørd123",       # literal
            "P@sswrd123",        # missing char variant
            "LongPassword123!@#$%",
            "Pa$$w0rd!",
            "Valid123!",
        ]
    
        def try_login(password):
            pwd_hash = hashlib.sha256(password.encode('utf-8')).hexdigest()
            try:
                r = requests.post(
                    login_url,
                    json={
                        "email": "aravind.m@gndsolutions.in",
                        "authMethod": "PASSWORD",
                        "password": pwd_hash,
                        "remember_me": True
                    },
                    headers={"Content-Type": "application/json"},
                    verify=False,
                    timeout=10
                )
                if r.status_code == 200:
                    return r.json().get("token")
                print(f"DEBUG: Login failed for '{password}'. Status: {r.status_code}, Body: {r.text}")
                return None
            except Exception as e:
                print(f"DEBUG: Exception during login for '{password}': {e}")
                return None
    
        # 1. Try default password first
        print(f"DEBUG: Trying default password '{default_pwd}'...")
        token_str = try_login(default_pwd)
    
        if token_str:
            return json.dumps(token_str)
    
        # 2. If failed, try rescue
        print("DEBUG: Default login failed. Attempting rescue with known passwords...")
        found_pwd = None
        rescue_token = None
    
        for pwd in known_passwords:
            if pwd == default_pwd: continue
            print(f"DEBUG: Trying candidate '{pwd}'...")
            t = try_login(pwd)
            if t:
                print(f"DEBUG: Found working password: '{pwd}'")
                found_pwd = pwd
                rescue_token = t
                break
    
        if not rescue_token:
>           raise RuntimeError(f"FATAL: Could not login with any known password. Please manually reset account.")
E           RuntimeError: FATAL: Could not login with any known password. Please manually reset account.

conftest.py:81: RuntimeError
=========================== short test summary info ===========================
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[valid_update_all_fields] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[no_changes_detected] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_all_fields] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_name] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_email] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[missing_phone] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[name_whitespace_only] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[name_single_char] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[name_max_length_exceeded] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[name_with_numbers] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[name_with_special_chars] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_format] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_no_at] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_no_dot] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_email_domain] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[email_uppercase] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_alpha] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_special_chars] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_short] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[invalid_phone_long] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[phone_starts_with_0] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[phone_all_zeros] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[payload_with_extra_fields] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[payload_null_values] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[payload_incorrect_types] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[empty_string_name] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[empty_string_phone] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[empty_string_email] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[update_phone_only] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
ERROR tests/test_update_profile_ddt.py::test_update_profile_ddt[unauthorized_no_token] - RuntimeError: FATAL: Could not login with any known password. Please manually reset account.
============================= 30 errors in 2.46s ==============================
